package org.goodev.kotlindemo

/**
 *  * 一、
 * 需要org.jetbrains.kotlin:kotlin-stdlib:1.2.10 库，
 * 包含 6692 个函数
 * jar 包大小 931KB
 *
 * 二、
 * Kotlin 代码编译为 Java 字节码和自己写的Java 代码其实是一样的。除了上面的额外引入的库意外，
 * 在运行的时候并没有什么不良影响。
 *
 * DataKotlin.kt
 *
 * 三、
 * kotlin-android-extensions 是一个针对 Android 的扩展库，目前主要的功能就是根据布局文件自动生成
 * 对应的 View， 这样用起来就不用每次去写一次 findViewById() 了.
 *
 * 在 Gradle 文件中通过下面的方式来应用这个插件，
 * apply plugin: 'kotlin-android-extensions'
 *
 * 然后在 代码中就可以直接使用 view 了， MainActivity
 *
 * 四、
 * Java 代码转换 Kotlin
 * Code 菜单下面
 *
 */

